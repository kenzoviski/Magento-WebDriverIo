{
    "configurations": [
        {
            "type": "node-terminal",
            "name": "Run WebDriverIO",
            "request": "launch",
            "command": "npx wdio run ./wdio.conf.ts --spec login.ts",
            "cwd": "${workspaceFolder}"
        }
    ]
}